Context: I am working on the "Smart Material Estimator" Streamlit application. Review app.py and allocation_engine.py.
🛑 CRITICAL INSTRUCTION: PLANNING MODE ONLY 🛑
Do not write or edit any files yet. Act as a Senior Python Enterprise Architect. Read my 9 requirements below, perform a full architectural analysis, write out a step-by-step plan, and wait for my explicit approval.
The Goal: Upgrade the app to "Smart Material Estimator & Planner" by adding an Order Management system, a batch-grid consumption engine, global location calculations, and advanced multi-view inventory dashboards.
Requirements to Plan For:
1. App Title & Tab Renaming
Change the main project header title to "Smart Material Estimator & Planner".
Rename the tab currently named "Equipment entry selection" (or similar) to "Selective Equipment Entry".
2. Location Report Enhancement (All vs Location)
In the Location Report tab, add a Radio Button: ["All Equipment", "Location Based"].
If "Location Based" is selected, keep the current logic.
If "All Equipment" is selected, bypass the grouping by location and calculate the materials and shortfalls across all equipment globally using the available materials.
3. Total Overview - Formatting & Sub-Metrics
In the Total Overview page, enforce exactly 3 decimal places for the "Lining Area SQM" column (currently showing 6).
Near the title "MATERIAL DETAIL BY SYSTEM CODE", inject dynamic sub-metrics explicitly showing: Total SQM Done / Total SQM, Total Pending SQM for that specific system code.
4. Advanced Consumption Grid & Validation
Validation: When entering consumption, calculate the theoretical max allowed based on the recipe's for_1_sqm * SQM entered. If the user's entry is over or under this amount, flag it in the UI (Over consumption / Less consumption).
Draft Grid: Replace the instant-submit button with an "Add to Grid" button. This must save the entry to a temporary space (use a robust state mechanism or a draft_consumption SQLite table so it survives if the app reloads) and display the pending entries in a grid.
Submit & Report: Add a "Submit Consumption" button. This writes the grid to the main consumption_log, clears the grid, and automatically generates a multi-sheet Excel report.
Multi-Sheet Excel: Sheet 1 = Whole day's combined entry. Subsequent Sheets = Split by System Code. Must include a column explicitly stating "Over Consumption" or "Less Consumption".
5. Main Inventory Dashboard (Read-Only)
In the Inventory page, add a "Main Inventory" radio toggle.
Add Date Filters (From Date, To Date).
Display a calculated table showing: Receipt, Consumed, Current Stock, Ordered.
Provide a multi-view toggle:
Grouped by System Code (showing material details beneath each).
Grouped entirely by Material.
6. Order Management System (New Database Logic)
Update initialize_empty_database() to include a new orders_log table tracking order_id (Unique), date, generated_by, material_code, qty, etc.
In the Inventory page, add an "Ordered" radio toggle.
Allow users to generate orders based on "Location needs" or "System Code needs" (pulling from the allocation shortage calculations).
Add a "Submit Order" button. This saves to the DB and downloads an Excel Order List (including the Logo, Date, Generated By, Order ID#, Material Code, Name, UOM, Qty) with formatted headers.
7. Receipt Linkage to Orders
In the Receipts entry form, add a text/dropdown box for Order ID (Optional).
If an Order ID is entered during a receipt, it must track against the original order.
In the "Ordered" view, add a dropdown to select past Order IDs to view their status (Ordered vs Received vs Pending) and download an updated status Excel report.
Your Task:
Write a step-by-step architectural plan. Specifically, detail your schema design for the new orders_log and draft_consumption tables, and how you will handle the multi-sheet Excel generation using xlsxwriter. Wait for my approval before executing any code.