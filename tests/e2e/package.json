{
  "name": "gi-hub-e2e",
  "private": true,
  "version": "0.0.1",
  "description": "Headless Playwright E2E suite for the GI Hub new stack (React SPA + FastAPI + Postgres). See README.md.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/node": "^20.14.0"
  }
}
